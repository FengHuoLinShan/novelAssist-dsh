---
name: novelcraft-interaction
description: "NovelCraft RP 私域(M4): 独立领域, 延后至 R6 后单独一档(D23)。RP 类任务先读本 skill。"
whenToUse: RP 故事会话、隐藏项目、回顾时。
---

# NovelCraft RP 私域(M4)

- RP 是独立领域(不与作者资产雷达混用); 数据面延后(R6 后单独一档, D23)。
- `deferred`: 当前没有 RP spec、领域包、持久旅程工具、分支/回顾/恢复或 client 流程。
- companion preset 只是明确延期占位, 不提供 Shell/文件能力, 也不模拟“已经保存”。

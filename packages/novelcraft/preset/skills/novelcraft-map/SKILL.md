---
name: novelcraft-map
description: NovelCraft 剧情地图(M4): map observations/空间事实 → 剧情雷达与 Story Map 读面。地图类任务先读本 skill。
whenToUse: 涉及地图状态、空间事实、剧情地图(Story Map)展示时。
---

# NovelCraft 剧情地图(M4)

- map observations 是派生读面(源数据在 scenes/memory/世界对象); 文件真相优先。
- 空间事实/规划内容步: llm_step(spec 待注册, map 域延后)——挂载阶段前不承诺。
- Story Map(§17.5): 章 × 线索 × Scene 时间线视图; 数据来自剧情雷达读面
  (memory.projectWorldState + outline 结构)。
- 纪律: 地图展示不改资产; 修订提案进收件箱。

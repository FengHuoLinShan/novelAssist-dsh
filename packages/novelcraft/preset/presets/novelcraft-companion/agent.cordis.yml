# NovelCraft RP 陪伴代理 — 基于官方 minimal 样例的最小组合(工具面按需裁剪)。
- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    text: "NovelCraft RP 持久旅程已按 D23 延后至 R6 之后单独立项。当前 preset 仅保留产品占位，不提供故事生成、分支、回顾、持久化、文件或 Shell 能力；请如实说明当前不可用，不要模拟已保存旅程。"
    complete: true
    includeRuntimeContext: false

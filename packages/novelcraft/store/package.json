{
  "name": "@novelcraft/store",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=24.11.0"
  },
  "description": "NovelCraft M4 R1 kernel: frontmatter validation, adopt+commit, CAS, merge/split/attach_alias, index rebuild",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run --exclude test/transaction-execute.test.ts && vitest run test/transaction-execute.test.ts --pool=forks -t 'ADR-0021 事务执行器|ADR-0021 fail-closed 前置|ADR-0021 复核点与 CAS|ADR-0021 崩溃恢复' && vitest run test/transaction-execute.test.ts --pool=forks -t 'ADR-0021 state / run_bootstrap|state/checkpoint|ADR-0021 intent 校验|复审 Blocker|N32 最终复审'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@novelcraft/memory": "*",
    "@novelcraft/vault": "*",
    "yaml": "^2.5.0"
  }
}

{
  "name": "novelcraft-m4",
  "private": true,
  "version": "0.0.1",
  "workspaces": ["packages/novelcraft/*"],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}

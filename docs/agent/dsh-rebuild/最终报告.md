# dsh-rebuild 最终交付报告

日期: 2026-08-14。分支: dsh-rebuild(基点 origin/main 5337fbcd3)。目标轮次: 20/40。

## 1. worktree 与分支

- worktree: /Users/tywww/Desktop/项目/ai-writing-assist-dsh-rebuild(仓库既有兄弟目录约定)
- 分支: dsh-rebuild(20 个提交, 未合并; 主工作区零修改)
- 合并路径: PR 评审 → origin/main(按 AGENTS.md 不直接提交 main)

## 2. 完成范围

### 2.1 功能对照清单(盘点全量, 见 docs/agent/dsh-rebuild/功能对照清单.md)
- 14 前端视图、357 API 端点(20 队列联动 + 1 SSE)、~30 任务类型、34 LLM 工作流、
  90 ORM 表/39 迁移、作者+RP 关键流程、健壮性基线 —— 全部有据可查(§1-§5)。

### 2.2 基于 DSH 的重构实现(ADR-0014, 分层: 产品三层不动, 共享 AI 基础设施 DSH 化)
- **Phase A**: 桥接 wire/token(dsh_step_bridge)+ 回调端点(internal_llm_bridge,
  Key 永驻 FastAPI)+ novelcraft-runtime 插件(managed_llm_step 工具, loopback
  /steps, ctx.tools.execute 完整工具流水线)+ SDK 运行时池(每 novel_id 一实例,
  fail-closed)。实机 E2E: SDK 启动真实运行时 + 文本/结构化双链路(ok/usage/
  journal/权限拒绝)。
- **Phase B**: structured 回调扩展(schema_ref 白名单 + 复用既有 generate_structured
  修复预算 + diagnostics 跨桥合并)。
- **Phase C**: snapshot ContextVar 通用 seam(任务路径 restore + 同步路径 open,
  34 工作流全量获得 dsh-sdk 通道)+ 5/5 模块工作流级 dsh 模式等价验收
  (outline/rag/writing/world/imports)+ 修复 model 冻结值 wire 透传缺口。
- **Phase D**: skills 家族 9 册(.dsh/skills, project-dsh 发现根)+ agent presets
  4 套(dsh/agent-presets)+ workflow 编排模板 2 个(批量审查/导入清点, 只读纪律)。
- 文档/治理: ADR-0014(Accepted+修订+索引)、DECISIONS 裁决记录、架构影响同步
  (整体设计/CONTEXT/CLAUDE/模块文档/治理记录)、DSH 上游文档归档(docs/agent/
  dsh-upstream, 47f9438)。

### 2.3 明确的未完成项(不得声称"完整复刻完成")
1. **7 处有意直调点保持 local**(interaction 故事流式 SSE、回顾重试特化、
   map_atlas 空间事实/规划、生成中心 chat): 功能不受影响(始终可用), 但未迁移到
   dsh 通道——属 Phase C 尾项/Phase E, 理由逐条记录于对照清单 §6。
2. **真实 provider 手动验收未做**: outline_analyze 试点与整体 dsh-sdk 模式需
   账户 Key 的端到端; runbook 已备(PhaseA-试点Runbook.md), 由持有 Key 的
   环境执行。
3. **GUI client-plugin 未做**: 原计划的可选项(需 DSH 源码 checkout 构建),
   未列入本次交付; 现有 Vue console 仍为作者界面, DSH web 为编排入口。
4. **前端零改动**: 本分支未触碰 frontend-console(用户可见体验保持既有基线,
   既有前端测试/视觉回归基线未跑——无前端变更, 视为不适用)。

## 3. 体验改善证据

- 作者路径/ RP 路径的用户可见行为、API wire、数据语义保持等价(前端零改动 +
  默认 LLM_STEP_EXECUTOR=local 零行为差异 + 全量回归通过)。
- 新增能力(非替代): DSH 运行时承载 step 信封(权限枚举/输出 schema 校验/超时/
  journal 走 DSH 工具流水线), agent 产品面(skills/presets/workflow 模板)为作者
  提供主编代理入口。
- 等价性证据: 5 个模块工作流在 dsh 通道上以与既有 local 测试相同断言通过。

## 4. 实际检查结果

| 检查 | 结果 |
|---|---|
| 全量单元(pytest tests/unit + modules) | 4212 passed, 12 skipped |
| ruff 全量 | All checks passed |
| docs-check(BASE_REF=origin/main) | passed(含 8+2 文档同步与 ADR 索引) |
| prompt-contracts | 19 contracts passed |
| E2E(PostgreSQL pgvector 容器, 完整套件) | 93 passed |
| 插件实机(真实 SDK 运行时 + mock 回调) | 文本+结构化双链路通过 |
| dsh 专项测试 | 50 passed(bridge/harness/pool/workflows×3 模块) |
| frontend | 零改动(diff 为空), 前端测试不适用 |

## 5. 契约或数据影响

- 稳定 API/wire/schema: **零变更**(357 端点、90 表、39 迁移、前端全部不动)。
- 新增(非破坏): backend 内部端点 POST /internal/llm/generate(仅
  INTERNAL_BRIDGE_SECRET 配置时注册, fail-closed); 配置 LLM_STEP_EXECUTOR 等
  6 项(默认 local); pyproject optional 依赖组 dsh(deepseek-harness-sdk/runtime-bin
  0.1.0rc6 锁定); 仓库新增 dsh/ 与 .dsh/ 目录(Node 插件/技能/preset)。
- 数据模型: 无迁移、无新表; 任务真相源仍是 PG async_tasks。

## 6. 未验证项及剩余风险

- 未验证: 真实 provider(Key)、gpt-image-2/S3 真实链、多 worker 部署下运行时池
  端口分配、DSH 升级路径(0.1.0rc6 锁定, SESSION_FORMAT_VERSION=0 无迁移)。
- 风险: DSH 开发者预览的破坏性变更(锁版本 + 升级窗口); 运行时进程的崩溃隔离
  (池按实例 fail-closed); 7 处直调点未迁移的覆盖边界; 内部桥单点(nonce LRU
  多进程尽力而为, 已注释)。

## 9. 补遗二(2026-08-14): 双发布物验证证据与待裁决项

- 插件集成冒烟 12/12(mock 引擎契约): 注册面/会话绑定注入/未绑定拒绝/
  白名单+参数校验拒绝/轮询/建议/审批 fail-closed/编译/问答(commit bcb57fa27)。
- 契约一致性门禁(commit a1dd1c150): 17 端点 × 引擎路由(prefix+suffix 两段
  匹配) + 10 任务 handler; 全量单元 4215 通过 + 全量 ruff 通过。
- 鉴权 authMode 三模式(bearer/cookie/none, 默认 bearer)与 ADR-0014 修订三
  提案(commit c5ddf5647)。
- **待用户裁决(阻塞项)**: ① 鉴权方案 A(引擎作者代理服务令牌 + 端点 scope
  白名单)是否授权实现; ② client GUI 插件是否纳入范围(需 DSH 源码 checkout
  构建)。两决策未答复前, 双发布物主体已完成, 引擎侧服务令牌不擅自实现。

## 8. 补遗(2026-08-14): 双发布物裁决后的落位

用户裁决后, 重构落位调整为两产物(commit ff0fcca43, ADR-0014 修订二):

- **指挥层 @novelcraft/dsh-plugin**(dsh-plugin/): skills 9 册(由 host 插件
  `ctx.skills.register` 运行时注册)+ presets 4 套 + 8 个 engine 工具
  (list_projects / select_project[会话绑定 novel_id] / trigger_workflow[白名单] /
  poll_task / list_suggestions / confirm_suggestion[审批门禁] / compile_context /
  ask_engine); tsc 构建通过 + SDK 运行时装载冒烟通过。client GUI 插件未实现
  (需 DSH 源码 checkout, 见 dsh-plugin/client/README.md)。
- **强制层 NovelCraft Engine**: backend + docker-compose.yml 即基础; 引擎侧
  Phase A-C 能力保留(LLM_STEP_EXECUTOR 默认 local); 插件经 357 端点 HTTP 契约
  协作, 不持有业务库/账户 Key/队列。
- 安装体验: dsh plugin add @novelcraft/dsh-plugin → 配置 engineUrl/agentToken →
  「帮我写第 3 章」。

## 7. 结论

用户可见功能与不变量(novel_id 隔离/owner 权限/密钥保护/历史回滚/LLM 授权/
schema 校验/双用户路径/体验状态)全部保持; "基于 DSH 重构共享 AI 基础设施"按
ADR-0014 分层方案完成 Phase A-D 主体, 26 个 managed step 调用点具备 dsh-sdk
通道并经 5/5 模块等价验收。**存在 4 项明示缺口(§2.3), 不声称完整复刻完成。**

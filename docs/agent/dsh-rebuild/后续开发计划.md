# 后续开发计划(M10–M12, 到 R6 前全部 P0+P1)

> 制定: 2026-08-31, 基线 novelAssist-dsh `9667cf06`(99 测试文件全绿)。
> 优先级依据《功能对照清单.md》§6.24(按用户效果); 源系统能力参照
> 《能力图-ai-writing-assist.md》(8c1516daf)。
> 本计划是活文档: 每批次完成后更新状态勾选与实际落点; 方向性变更须记 N 裁定。
> 2026-09-01 全景诊断后的增量施工顺序见《[项目评估图谱-修复优化交接计划](./项目评估图谱-修复优化交接计划.md)》；后者不改写本文件的历史完成记录。

## 0. 插件边界纪律(统领所有批次, 违反即 gate/测试红)

依据: 上游 deepseek-harness AGENTS.md("Plugins, not loop changes"、"Registrations are
effects"、模型可见⟺可回放、可调参数走 Config、失败要响)+ 本仓「一切皆插件」A–H 整改
+ ADR-0024 capability 三分法 + 架构铁律 1/3/4。

每项新能力必须落在正确的层与扩展点上:

1. **core 包只做加法**: 新文件 + 新导出(照 B 阶段 `1fb45fa0`、H 阶段 `commitScenesTx`
   先例); 不改既有签名/行为路径, 旧路径保留为回退; 零 `@deepseek-ai/*` 运行时依赖。
2. **dsh 适配层走 seam**: 服务读取只经 `svc(ctx,…)`; 适配器 fail-closed; 一切注册经
   `ctx.effect()/ctx.on()` 返回 disposer; 默认工具组装由 `ctx.inject(['tools'])` 驱动同步
   register，避开 rc.8 嵌套 `ctx.plugin` 静默吞错坑; jobs 走 JobKindMap 声明合并 + 遵守 AbortSignal。
3. **能力语义归位**(ADR-0024 声明表, 每能力只落笔一次): 只读→`read`;
   写待处理/草稿→`propose`; canonical adopt/status/升格→`adoptGuarded`
   (allowed-once fail-closed)。raw 写面不进公开面。
4. **新工具 = 新工具组插件**: `src/tools/<组>.ts` 经 `novelcraftToolFactory`
   (N34 隔离/toolError/封闭 output schema); 执行器只调
   `run.service.capabilities.*`/`run.service.vaults.*`; 变更副作用只经
   `run.afterMutation()`; `config.tools.<组>` 开关走 schemastery Config;
   独立插件仅经 `@novelcraft/dsh/internal` 导出。
5. **同步四处硬清单**: 隔离矩阵 `dsh/test/workspace-isolation.test.ts` MATRIX
   (含完整性自检)、preset 内联工具名清单(`preset/test/preset.test.ts`)、
   dsh README seam 矩阵、交接文档 §4.2 口径; 相关 spec/README/JSDoc 随代码走。
6. **模型可见 ⟺ 可回放**: 任何改变发给模型内容的改动(Prompt 正文/schema 注入)必须把
   prompt hash/注入模式进 journal/trace, 行为测试断言可重建。
7. **client 面零 canonical 写**: 新读面只走 `service.ui`(client-face)只读聚合/收据
   暂存; client 禁核心包值导入与裸 node:fs; UI 文案走类型化字典。
8. **Git 写收敛**: 业务 git 写只准出现在 store 事务实现的允许表内
   (check:git-writers); 新写面接 N32 目标路径级事务, 禁 `gitAdd(root)`/`-A`。

负面清单(会被拒): execute 直调 `service.<方法>` 绕 capabilities、绕过
novelcraftToolFactory 手写 defineTool、绕 ApprovalGate 的 canonical 写、手调
fireRadar/fireRag/push 不经 afterMutation、core import @deepseek-ai、改 core 既有签名、
默认组装用嵌套 ctx.plugin、改共享层不过 ADR-0018 四条纪律。

## 1. 基线与排序

- 基线 HEAD `9667cf06`; P0 主链(导入→章节→审查→采用/拒绝)可用, 21 工具/14 client
  RPC/9 Skill。
- 排序按台账 §6.24(用户效果): P0 三缺 = LLM 内容步收口、长任务恢复面、多书生命周期;
  llm_step 信任面是 M12 生成域接线的地基, 故 M10 先行。
- 每轮门禁(AGENTS.md): `npm test` 全绿 + `npm run typecheck` 零错误 +
  check:distribution/check:git-writers/check:audit-gate 全绿 + trace contract 断言
  新增动作 + 台账对应切片状态更新。

## 2. M10: P0 收口轮(任务级) — 进行中

### Track A: LLM 内容步 runtime 收口(§6.23)

| # | 任务 | 层/落点 | 状态 |
|---|---|---|---|
| A1 | 宿主能力探测(闸门): rc.8 `ctx.llm` seam 是否支持 response_format/JSON Schema 能位; 不可用则记 N 裁定降级 schema 文本注入 | 只读探测 + 报告 | ☑ 2026-08-31, 见 §2.A-探测 |
| A2 | Prompt 正文加法: core `llm-step` 新文件(如 `prompt-body.ts`)+ spec 注册新可选 `promptBody` 字段; dsh `llm/provider.ts` 组装 messages 优先 promptBody, 旧 description/inputNotes 路径保留回退; **不重构 systemPromptFor** | core 加法 + dsh 适配 | ☑ 落点修正: 组装收敛在 core `prompt-body.ts`(composeSystemPrompt), DshProvider 只透传 system, dsh 零改动; legacy 摘要文本字节级不变(内联 golden 测试)——但仅 text 形态整体不变, legacy **json** spec 自 N38 起追加 OUTPUT_CONTRACT 注入(目的性行为变更) |
| A3 | schema 首轮注入: messages 组装处加法式注入(原生 response_format 或文本降级, 两模式行为测试); 注入模式与 prompt hash 进 journal(纪律 6) | core 加法 + dsh 适配 | ☑ 文本注入模式(N38), text 形态不注入; journal 每条 promptHash+schemaInjection, 8 项行为测试 |
| A4 | 回执去截断: `novelcraft_llm_step` output schema 扩展回传 journal/profile/usage(去 8000 字截断) | dsh tools.ts | ☑ text 不截断 + journal 逐字段 JSON 投影 + spec_ref/contract_version + 三指纹字段 |
| A5 | exact-route readiness: `llm/preset.ts`/`provider.ts` 经 `svc(ctx,'llm')` 读宿主 route 状态, 区分 selected/configured/ready; 未 ready fail-closed 带原因(§6.23.6); 遵守显式 resolve(request) 默认值模式 | dsh 适配 | ☑ 落点修正: 检查在 DshProvider.complete(llm.stream 前)——listProviders live 目录核对, selected≠ready 提前拒并回传 live 目录; 全局唯一 ctx.llm 接触点, 编排/裸 runStep 全覆盖 |
| A6 | 生效调用参数与恢复指纹最小回执(§6.23.9) | dsh + journal | ☑ StepEffectiveParams(合并链终值)进 StepResult 成功/失败(budget 阶段除外——参数未组装完); llm_step 工具回执扁平化 effective_* 五字段; 指纹+参数双回执 |

验收: trace/journal 断言 prompt_hash+注入模式+预算; 负例(未 ready、schema 校验失败
重试、promptBody 缺失回退); 台账 §6.23 状态更新。

**Track A 整批 review(2026-08-31, 独立评审代理)**: 无 P0; 1 项 P1 + 7 项 P2 全部修复
(N39): ①P1 llm_step 工具 max_tokens/timeout_ms 零值吞没(truthiness→显式 undefined);
②golden 断言循环引用→内联硬编码字面量全等; ③回执上界走 Config.llm.receiptMaxChars
(缺省 65536, 经 capabilities.read.receiptLimit 读取——工具不直读 config, N35);
④预算口径: 单步输入守卫(checkBudget)保持 req.input 旧口径, workflowBudget 累计
口径并入 system 提示估算(避免输入上限与输出预算耦合误拒); ⑤effective 缺省分支断言
(Config 路由填入); ⑥trace LlmStepEvent 加 promptHash/schemaInjection 可选字段 +
ProviderRequest 透传 + imports 两处 tracedProvider 转发; ⑦JSDoc 携带范围措辞修正;
⑧计划 A2 限定语(仅 text 形态字节级不变)。连锁修正 4 处预算边界测试(公式加 system
估算, 测试意图不变)。门禁: 16 包全绿(world 包全仓并发时出现一次 vitest worker
onTaskUpdate 偶发超时, 单包复跑 151/151 干净, 记录为基础设施偶发)。

#### §2.A-探测: A1 宿主 llm seam 能力探测报告(2026-08-31, rc.8)

证据锚点: `node_modules/@deepseek-ai/dsh-llm/lib/types/types.d.ts:332-383`(GenerateOptions
完整字段面)、`types/index.d.ts`(LlmRuntime/LlmAdapter 服务面)、
`packages/novelcraft/dsh/src/llm/provider.ts`(现有适配)。

1. **GenerateOptions 无 response_format/json_schema/structured 字段**。完整字段面 =
   provider/model/reasoningEffort/messages/system/tools/temperature/maxTokens/stop/
   signal/sessionId/purpose(`purpose` 为封闭枚举 `'compaction'|'session-title'`,
   llm_step 调用不能标新 purpose)。rc.8 执行契约不支持原生结构化输出请求参数。
2. **存在 `tools?: ToolSchema[]` 原生通道**(adapters map to provider's `tools` field;
   ToolSchema={name,description,parameters:JSON Schema}), 但**不采用**: ①把内容手输出
   伪装成 tool-call 超出宿主对该 seam 的文档化用途(agent-loop 工具语义面); ②DshProvider
   现只消费 text-delta/usage/finish, 走 tools 需新增 tool-call 块解析与失败路径,
   复杂度高而结构遵循度收益未证实; ③模型可见⟺可回放纪律下伪装工具会污染 session log
   的工具语义。
3. **schema 文本注入路径成立**: `system` 槽为纯文本(adapters map to provider's system
   slot), DshProvider 已直接透传 —— OUTPUT_CONTRACT(JSON Schema 文本)进 system 尾部
   = 零宿主改动、零共享层触碰、llm-step 加法即可。
4. **A5 readiness 接入点(顺带确认)**: `LlmAdapter.listModels(provider)`(advisory,
   "absence must not become request rejection")、`LlmAdapter.resolveModel(provider,
   model, signal)`(exact-model 元数据, 不校验路由)、prepared-call 两段式
   (call-config 匹配校验 `INVALID_PREPARED_CALL`)、DirectoryRegistrationHandle
   (live/dormant provider directory)。
5. **N38(草案, 随 A2/A3 实施正式记入 specs/adjudications.md)**: schema 首轮注入采用
   **schema 文本注入**(OUTPUT_CONTRACT 进 system 槽), 不使用 GenerateOptions.tools
   伪装工具, 不触碰共享层加 response_format。去条件: 宿主 dsh-llm 契约新增
   response_format/structured 字段时重新探测; 或实测文本注入结构遵循度不足且经
   产品裁决接受 tools 通道复杂度。

### Track B: 长任务作者恢复面(§6.9+§6.6)

| # | 任务 | 层/落点 | 状态 |
|---|---|---|---|
| B1 | 新工具组 `tools/workflow.ts`(§0 纪律 4 完整姿势): `novelcraft_workflow_inspect`→`read`(接 core `resume.ts:resumeImport`/run-engine 既有导出, core 零改动); `_resume`/`_abandon`/`_start_new`→`adoptGuarded`(经 ApprovalGate); service.ts 新方法 + capabilities.ts 声明表登三项 | dsh 新工具组 | ☑ core 加法 run-listing.ts(N40); workflow-face.ts + 4 工具 + NovelcraftWorkflowToolsPlugin + config.tools.workflow; 落点修正: inspect 复用新 listWorkflowRuns(两 namespace 枚举)而非单 run classifyExistingRun |
| B2 | completed replay 修正: 相同 completed 输入不再隐式重放, inspect 返回"重放 vs 新 run"显式选择(§6.6.2) | dsh + core 读面 | ☑ DeepImportOptions.force(加法)→ uniqueRunId 时间戳段; start_new 全 scope 授权; 旧 run 不被复用 |
| B3 | deep-import 去同步长阻塞: `novelcraft_deep_import` 改启动 run+返回句柄; 执行转新 job kind(JobKindMap 声明合并 + AbortSignal + fingerprint 重启, 照 `jobs/radar.ts` 模式; ADR-0023 单次补跑语义) | dsh jobs | ◐ 后置独立增量(N40 v1 限定): resume/start_new 暂同步执行(工具 1h 超时), inspect/abandon 已可闭环; job 托管须对读 ADR-0023 执行节后设计 |
| B4 | client 读面: `service.ui` 新增运行中工作流卡只读聚合(零 canonical 写; 文案走字典; §6.9.2 最小投影) | client-face | ☐ 后置与 B3 同批(工作流卡的价值依赖 job 托管后的异步运行态) |
| B5 | 同步四处: MATRIX 增 4 工具、preset 工具名清单、README、交接 §4.2 | 测试/文档 | ☑ 矩阵 25 + preset 25 清单 + README + 交接 §4.2 + capabilities/plugins 契约与 fakeService/adoptGuarded 清单 |

验收: 中断→inspect→resume→幂等行为测试(FakeApproval 三态); trace 断言 run 生命周期
事件; 工具组契约测试(开关/单独挂载/卸载 dispose); 台账 §6.6/§6.9 更新。

### Track C: 写面收敛(§6.24.1)

| # | 任务 | 层/落点 | 状态 |
|---|---|---|---|
| C1 | `generate_next_chapter` + `map_atlas_upload` 写面接 N32 事务: core 加法(新 Tx 变体导出, 照 commitScenesTx 先例)+ orchestrate 切换调用 | core 加法 + dsh | ☑ 落点修正: 「接现有复核」的最小正确读法 = store 加法 hasStagedOutside(N41, 只挡预存 staged——裸 commit 实际卷入面), 四处写面 gitAdd 前门禁(generate 双门禁: LLM 前 + commit 前复检; atlas write 三处); generate 已有 wx 独占创建 + 精确 pathspec 既有原子性保留 |
| C2 | ingest 完整 writeSet 收口 + signal state/CAS | dsh + core 读面 | ◐ 主体已在 3b30fb9d(N32 素材入库补偿回滚+精确 commit)与 radar reconcile plan→changed-only 落地; signal CAS 强化与 RAG change plan 合并后置(见 C3) |
| C3 | 删无 consumer 的 index cache 写; RAG 派生 writer 改 change plan→fresh 零写 | core 加法 + dsh hook | ◐ 半项完成: refreshIndex 死写已删(putIndex 零消费者, 能力面保留); RAG change plan 重构后置(台账 §6.24.1 的完整要求: fresh 零写/stale|missing|corrupt 才 atomic replace/chunk_id 唯一/exact-slice 等, 独立增量) |

验收: check:git-writers 通过; 事务部分失败回滚负例(faults 注入照 H 阶段契约测试)。

**Track C 整批 review(2026-08-31, 独立评审代理)**: 2 项 P1 + 6 项 P2 全部修复(N41):
①P1 hasStagedOutside rename 双端 OR 判定 fail-open(单端豁免可放行范围外源文件删除卷入)
→ AND 语义 + 单测矩阵锁定; ②P1 generate 门禁在 LLM/落盘之后(失败搁浅候选文件 +
误导性重试报错)→ 双门禁形态(LLM 前置 + commit 前复检); ③P2: abandon 豁免集补目录
前缀(run 内 staged 残留不挡清理目标)/refreshIndex JSDoc 同步/旧容忍用例名与死变量
清理/hasStagedOutside 矩阵单测(5 用例: untracked-unstaged 放行、:(literal) 剥离、
rename AND、目录前缀、staged-in-exempt 幂等)/generate 门禁与幂等重试测试。review
另列出其余裸 commit 点清单(world/generation、writing/review、revise、import-text、
outline/structure)——后置与 RAG change plan 同批(N41 ④)。

Track 间无硬依赖; Track B 不依赖 A 可先行。

**Track B 整批 review(2026-08-31, 独立评审代理)**: 1 项 P0 + 4 项 P1 + 6 项 P2 全部修复
(N40 追记): ①P0 abandon 路径穿越(模型可控 workflow_id 直拼 rmSync, 可删 vault 外目录)
→ 单段校验 + 枚举存在性双门, 穿越/不存在 id 零审批零删除(负例测试锁定); ②P1 abandon
缺 R17 洁净门禁(预存 staged 会被裸 commit 卷入)→ hasUncommittedOutside(removed 豁免)
前置 + 仅终态可清理 + 摘要明示 intent 孤儿风险; ③P1 resume 绑定"咨询性"(请求 id 从不
与实际续跑对账, 漂移静默变新 run)→ 枚举存在/非 force run/checkpoint 绑定三重前置 +
执行后对账(WORKFLOW_RESUME_DRIFTED); ④P1 "不再隐式重放"言过其实 → deep_import 描述
加指引 + N40 措辞修正(显式路径新增, 旧隐式路径保留); ⑤P2: force 随机熵/README 插件
枚举/ADOPT_PAIRS 语义注释/corrupt 文案通用化/inspect 描述收敛"manifest run"/commit
返回真 sha/审批透传 signal; 测试补穿越、非终态、staged 脏、atlas 枚举四负例+正例
(11 用例)。resume 成功路径与 force identity 差异的行为级测试随 B3 增量(记入 N40)。

## 3. M11: 多书生命周期(N42, 2026-08-31) — 核心完成

- [x] 公开书库生命周期: book 工具组 3 工具(list→read; create/open→adoptGuarded, 审批
  在 service 方法内——N35 纪律)+NovelcraftBookToolsPlugin+config.tools.book; 预绑定
  Vault 的 developer-only 挂载自此有作者可达入口(创建/切换/枚举)。
- [x] 删 client `workspacePath` fallback(rpc.ts resolveRoot 只认 sessionId; §6.13)。
- [x] 隔离形态裁定(N42): book 组是「未绑定也可用」入口, N34 矩阵加例外(情形 2/3 跳过
  book 组); 无 agent 拒; 不接受模型路径(书名经 rootForBook 防穿越)。
- [x] book.yml 消费者核查(§6.13.4): BookMeta 消费面=initVault 初始化 + binder.readVault
  的 title(会话绑定显示); 其余字段(genre/tone 等)全仓零消费者——**不在工具描述/文档
  宣称生效**, 后续有真实消费者才公开(台账 §6.13.4 原则维持)。
- 后置: books/list client wire(与 B4 工作流卡同批, N42 ④); 回收站不做(§6.13.6)。

**M11 整批 review(2026-08-31, 独立评审代理)**: 2×P0 + 1×P1 + 9×P2 全部修复(N42 追记):
①P0 工厂无条件 resolveBoundRoot 使未绑定会话被工厂层拒——「未绑定可用」不成立
(requireAgentForBooks 成死代码)→ bindRoot:'none' 第三模式 + requireRoot 收窄 +
未绑定正例; ②P0 client workspacePath 删除在首次提交静默失败(python 无 assert)
→ 已真正落地并反转旧回退测试; ③P1 book_open 不驱动守望 → binder 返回
deactivatedRoot + NodeRuntime.activate/deactivate 透传; ④P2: 校验先于审批/
open 摘要含当前书/矩阵注释重写/requireAgentForBooks 归属 tools/shared/
capabilities 注释错位/书名穿越负例。book-list 的 title 手搓正则与
parseFrontmatter 双轨(展示面瑕疵)与大小写不敏感 FS 同名风险记为已知边界。

## 4. M12: 生成域与证据链接线(N43-N47, 2026-09-01 完成) — 已收口

- [x] **M12-a(N43)**: worldCreate/worldUpdate 工具补齐(N31 能力入口化, writing 组,
  行为测试 + 四处同步 30)。
- **M12-a 整批 review(2026-08-31)**: 无 P0; 1×P1(entity_type 无白名单 → 工具层 fail-fast,
  core kind enums 记 M12-b)+5×P2 全闭(描述修正/死代码/空 patch 早拒/测试补强)。
- [ ] **M12-b(首要, N43 ②裁定推迟)**: outline 生成面与 world 生成中心五模式工具化前,
  core 必须先做 preview/apply 拆分(preview 不写资产 + 生成产物暂存面 + apply 显式
  adoptGuarded)——现状直写 canonical, 不拆分即冒充 preview(违 §6.17/§6.18.2)。
  另: core object schema 的 kind enums 补齐 + slug 坍缩错误语义(N43 追记)。
- [x] **M12-b 完成(2026-09-01, N44)**: preview/apply 拆分(outline preview.ts 暂存
  proposals + apply 白名单透传)、9 工具(outline×4 + world 生成中心×5)、kind enums
  (object 层收紧 + imports 归一化缝合 deep-import 链)、slug 坍缩区分。
  review(无 P0, 2×P1+7×P2 全闭)与已知边界见 N44 追记。

- **outline 工具组**(core 4 函数+5 specs 已就绪零改动): story_outline/outline_generate
  (P20)/analyze/scene_fusion preview→`propose`, apply→`adoptGuarded`。
- **world 生成中心工具组**: 五模式 chat/converge/explore/inspect/suggestBiblePage→
  `propose`; 补 `worldCreate/worldUpdate` 工具(能力已在 adoptGuarded 表注册, 只差入口)。
- **世界书 draft→publish**: 同名页修订冲突修正 + 历史(§6.17; publish→`adoptGuarded`)。
- **memory 接线**: core 零改动; 写链事件 append(经 adopt 后 afterMutation 挂钩)+
  按故事顺序投影只读面进 dossier/propose(§6.18.4)。
- **context 编译器接线**: propose/generate 调用路径消费 `compileContext`
  (P4 检索进写作链)。
- **RAG**: current-source typed result + open target(§6.12.5/6.12.6); L2 默认启用评估
  (ContentPreset 卡开关)。
- [x] **M12-c 首切片(2026-09-01, N45)**: context 编译器进写作链(propose/generate 输入经 Tier P0-P4 预算编译, 旧拼接版保留兼容)。
- [x] **M12-c 次切片(2026-09-01, N46)**: memory 接线 —— saveChapter 审批成功后事件 append
  (幂等键 chapter+seq=0, 快照含 hash/commit 不含正文, 容错降级); dossier 增按故事顺序的
  记忆投影读面(store→memory 新依赖, 拓扑无环)。review 修正: 自动写点回撤(违反
  §6.18.4 manual_correction reserved 裁定), 读面保留; 依赖门禁 3 处补声明(N44/N45
  遗漏一并修); 边界与后续写点方案见 N46 追记。
- 穿插体验迭代(交接 §7 条目 10 逐项评估: 收件箱批量采纳+undo、窄屏、修订块回 Word、
  真实一书手动验收、rebuildIndex 容错)。

## 4a. N48 clean-build 与插件边界复核(2026-09-01)

- [x] workspace build/default CI/BGE CI 改为满足 package.json 直接依赖的拓扑，distribution
  gate 新增 manifest 拓扑验证，锁定 memory→store 与 context→writing 两条回归。
- [x] M12 world(7)/outline(4) 从 writing 兜底组拆出；六组均有 Schemastery 开关与
  internal Cordis 插件，39 工具名称/schema/注册顺序不变。
- [x] 根服务以 `ctx.inject(['tools'])` 跟随 tools provider 生命周期；internal 插件硬依赖
  novelcraft+tools，仅供程序化组合，不新增 YAML subpath/npm 包。
- [x] 删除 dsh 的 context/memory 冗余直接依赖；N43/N44 仅「归 writing」口径由 N48 取代。

## 4b. 父仓对齐增量(2026-09-07, N52)

> 依据: `docs/agent/reviews/2026-09-07-parent-pr-alignment.md`(父仓基线推进至
> origin/main `fede3bd72`, 台账 §6.1/§6.20/§6.25 已同步)。本节只登记方向与判据,
> 不改变 §2–§4a 的历史完成记录; 各项开工前须细化到任务级(照 M10 格式)并遵守 §0
> 插件边界纪律。

- [ ] **P1 候选: client 窄屏/移动批提级**(父仓 #118 把手机完整工作流列为一等公民):
  含交接 §7 条目 10 的「窄屏角标+底部抽屉」与台账 §6.22 的 390px/焦点缺口。
  完成判据: 390px 视口下高频闭环(导入→审查→采用→返回创作位置)可用, 焦点行为
  可测; 不建 Vue 壳/client router(§6.22 既定边界不变)。
- [ ] **P2 候选: rc.1 主题能力探测**(照 A1 宿主探测先例, 只读探测 + 报告):
  探测 DSH rc.1 宿主 web 是否向 `service.ui` 插件面板暴露主题/外观 seam(CSS 变量、
  `data-theme` 类或等价面)。有 seam → 评估浅/深主题适配并记 N 裁定; 无 seam →
  truthful 不做(主题包机制 ADR-0019 属父仓独立前端壳能力, 本仓不做——N52)。
- [ ] **P2 候选: CI PR/main 分流评估**(学 #117 模式, 不学数字): N51 烟测链落地后
  先量 CI 各段时长; PR 路径瘦身为「受影响面 + 冒烟」、main 保持全量门禁。仅在
  实测 PR 等待成为瓶颈时实施; 不复制父仓 51 项数字。
- [ ] **P2 候选: 性能基线挂「真实一书手动验收」批**(父仓本地未推送的 world 读路径
  性能探针/基线是同一信号, 记候选区): 验收批测 story map/dossier/RAG search/
  reconcile 热路径并记基线数字; 不做预防性优化。
- 需用户裁决(仅记录, 不排程): **付费评测纪律**——父仓 #110 示范完整门禁形态
  (冻结 holdout + 标注模拟 reviewer + readiness 判定 + 失败即停不调参); 本仓已有
  零付费 36-call dry-run 框架, 付费版须先获费用/数据授权(沿用既有裁定先例)。
- 明确不对齐(防回流, 详见 N52): RP 记忆特性本身(D23); 认证前多入口(#109);
  主题包机制(#118/ADR-0019); 镜像 CVE 修复(#115, 无镜像分发, audit gate 已覆盖)。

## 4c. M13-A: 常驻创作状态面(N53, 2026-09-08) — 已完成

> 依据: 2026-09-08 wiki DSH 调研头脑风暴(用户选定 A1 方向)+ 宿主 seam 逐字探测
> (0.1.2-rc.1 `@deepseek-ai/dsh-system-prompt` 完整存在)+ 用户两项裁定(快照=精简 v1;
> `config.prompt.enabled` 默认开)。裁定全文与 Alternatives considered 见
> `specs/adjudications.md` N53。

- [x] **S1 core 加法**: `@novelcraft/context` 新文件 `resident-state.ts`
  (ResidentStateInput 自持类型/buildResidentState/renderResidentState 确定性渲染 +
  预算降级阶梯; 复用 estimateContextTokens, 不复用 compileContext 驱逐语义) + 单测
  (golden 字节级一致/截断/空容错)。
- [x] **S2 依赖与 Config**: dsh peer/dev 显式钉 `@deepseek-ai/dsh-system-prompt
  @0.1.2-rc.1`; dsh→`@novelcraft/context` 依赖恢复(resident-state 真实消费者,
  N48 删的是当时无消费者的冗余); `config.prompt.{enabled(默认 true), maxTokens
  (200–2000, 默认 600)}`。
- [x] **S3+S4 状态面模块**: `src/prompt-face.ts` —— section `novelcraft:usage`
  (order 300 静态)+ context `novelcraft:state`(order 130); text provider 按
  `ac.agent.session` 分支(`SessionVaultBinder.peek` 新增只读同步方法), 无 agent/
  未绑定→空贡献; 重算在请求路径外(激活预热 via composeActiveVaultRuntimes +
  afterMutation 漏斗 via `capabilities.read.refreshResidentState`(N35 扫描合规)+
  Git OID/逐信号元数据指纹漂移自愈, 一回合内收敛); 快照采集=storyMap+
  inboxView 只读(逾期伏笔判定与 radar-risk 同规则), 不含正文/绝对路径/记忆明细。
- [x] **S5 行为测试**: `test/state-face.test.ts`(真实 SystemPrompt 插件): 注册形态/
  无 agent 与未绑定空贡献/双书不串书/漂移自愈(旧值→新值)/stopAll 冷路径/
  enabled=false 零注册/maxTokens 上界/**工具面仍 39**。
- [x] **S6 四处同步 + N53**: 裁定 N53; dsh README seam 矩阵增 `ctx.systemPrompt` 行 +
  两条部署注记(includeRuntimeContext=false 快照不进历史; 外部直改滞后反映);
  交接 §4.2 口径; 本节。MATRIX/preset/工具面零改动(无新工具)。
- [x] **S7 整批独立评审**(2026-09-08): 无 P0; 2×P1 已修——①P1 重算同步段落在请求
  路径内(text provider/afterMutation 调用栈会同步跑完全量重扫)→ refreshNow task 首行
  `setImmediate` 让出同步段(inflight 去重窗口保持原子); ②P1 maxTokens 用例无检验力 →
  重写为真实超预算 fixture(5 条 60 字逾期伏笔)+ `estimateContextTokens ≤ 上界` +
  降级特征断言。2×P2 当时记后续: worktree/submodule 指纹退化与 assemble 指纹
  fs 读节流; 实体 `.git` 契约下的 mtime 漏检已由四轮改读 OID 关闭。
  评审另核实: compose 生命周期无回归/peek 无越权面/快照无正文路径/N35 扫描合规/
  `AssembleContext.agent` 声明合并真实存在/order 与文档一致。
- 分两个提交(①core resident-state+单测; ②dsh 注入+行为测试+文档+N53), 各附 Agent Note。
- [x] **S7 追记(二轮独立评审, 2026-09-08)**: 提交后另派独立评审代理复查(与一轮不同
  发现): 1×P1 已修——动态快照正文嵌 vault 自由文本(书名/章名/伏笔名/状态键)未中和
  `{{}}`; dsh-system-prompt 对 context 文本同样做严格插值, 章名含 `{{草稿}}` 即让该
  会话每次请求的快照渲染 throw(真实插件实证)→ core `renderResidentState` 出口统一
  `sanitizePromptVars`(零宽间隔; 预算口径按中和后文本, 硬截断不再产生新 `{{`)+
  双预算档回归测试。3×P2 同批修复: ①「逾期伏笔判定与 radar-risk 同规则」口径失实
  (快照额外排除 archived, 计数可与收件箱 risk 信号不一致)——代码/测试口径改正;
  ②漂移自愈测试原先显式 refreshNow 驱动、assemble 的 drift 分支零覆盖 → 改收敛循环
  真实走分支; ③stopAll 后 in-flight compute 仍写回缓存 → task 内 stopped 守卫+负例。
  P2 维持后续(不阻断): assemble 指纹节流; worktree 不在每书独立实体 `.git`
  仓库契约内, 不为它新增退化路径。
- [x] **S7 追记(三轮评审修复, 2026-09-08)**: 信号指纹枚举复用
  `assertNoSymlinkOnPath`, 不再由 `statSync` 跟随 vault 内 symlink; `setImmediate`
  前不再执行含同步 `git rev-parse` 的 vault 验证; core 拒绝小于 200 的
  非法预算, 硬降级仅截过长书名并保留书名行/章游标, 返回值严守上界。
- [x] **S7 追记(四轮整链复审, 2026-09-08)**: 状态计数改用 `Map`, 不再被
  `__proto__`/`constructor` 原型键吞掉; Git 指纹读取 loose/packed ref OID,
  信号指纹按每个 `.json` 的 name/size/mtimeNs/ctimeNs 聚合; deactivate 通过
  generation 阻止 in-flight 回写; afterMutation 在雷达信号落盘后才调度快照。


## 4d. M13-B: 结构维护可复算基准(continuity benchmark, N54, 2026-09-08) — 计划已入册待实施

> 依据: 2026-09-08 wiki DSH 调研头脑风暴(用户选定 A2 方向, 对应头脑风暴方向 A2)+
> 用户两项裁定(范围=基准底座+补确定性检测器, LLM 语义类只诚实标注; 捕获率仅报告
> 不设 CI 硬门禁)。裁定全文见 `specs/adjudications.md` N54。与 §4c(M13-A 常驻状态面)
> 互不依赖、可并行; 现状检测器面与缺口清单依据 2026-09-08 只读探索核实。

### 0. 插件边界纪律(A2 特化, 统领批 1/批 2)

1. **core 只做加法**: 新检测器 = assistant/outline 新文件 + 新导出(照 `run-listing.ts`/
   `commitScenesTx` 先例); sweep/health 编排处加法式接线, 不改既有导出签名与既有面
   行为路径。
2. **零 DSH 依赖**: 检测器全落 core; `scripts/continuity-benchmark.mjs` 零
   `@deepseek-ai/*` import(照 `benchmark-read-paths.mjs` 先例); dsh 层预期零改动
   (A2-2.5 核对后最多补一处事件映射)。
3. **读面语义**: 新检测只产信号进收件箱(`Signal` 既有结构), 不经 adoptGuarded; 必须
   满足对账契约——`logical_key` 业务身份稳定、`observation_hash` 绑载荷、裁决不复活、
   问题回来重开(复用 `reconcileRadarSignals` 路径), 防收件箱重复轰炸。
4. **Git 写收敛**: 信号落盘走既有 store state transaction, 无新 git writer
   (check:git-writers 允许表不变)。
5. **不新增工具/不增第七雷达面**: 新检测归入 risk/health 既有面, 39 工具与 MATRIX/
   preset 清单/README seam 矩阵不动。
6. **模型可见⟺可回放**: 信号经既有 health_scan/radar_sweep 工具可见; 基准断言 core
   直调结果, 与工具输出一致性由既有工具测试保证, 不重复建契约。

### 批 1: 基准底座(N54)

| # | 任务 | 层/落点 | 状态 |
|---|---|---|---|
| A2-1.1 | 错误注入目录 `evals/continuity-benchmark/catalog.json`(单一事实源, scripts 消费; vitest 不消费, 避免 dist 解析与跨包依赖门禁问题)。条目 = `{id, class, inject, expected:{detector, face, kind}}`, 三分类: **covered**(~15 条: failed_import、uncovered_chapter、L0 同名、alias 候选、thin_object、foreshadow_overdue、chapter_gap、显式 relations 悬空、health 六键各条、POV 缺失/歧义、knowledge future/invalid); **gap**(8 条, 批 2 目标: reveal_before_plant、thread_range_invalid、arc_thread_range_mismatch、thread_orphan、reveal_target_dangling、payoff_scene_dangling、legacy_edge_dangling、scene_index_conflict); **llm-only**(2 条: pov_violation_in_prose、knowledge_leak_in_prose) | evals/ 新目录(照 deepseek-v4-flash 形态) | ☑ 2026-09-08: 28 条(covered 18/gap 8/llm-only 2); gap 条目 id_prefix 一次定死为批 2 信号 id 契约 |
| A2-1.2 | 基准脚本 `scripts/continuity-benchmark.mjs` + `npm run bench:continuity`: mkdtemp 种子 vault(多章+Scene+thread/arc+对象+伏笔边, 复用 radars.test.ts/continuation-e2e 的 seed 模式)→ 逐条注入 → 直调 `runRadarSweep`/`scanHealthSignals` 等 core API → console.table 输出 类目×期望检测器×PASS/MISS/LLM-ONLY 矩阵 + 汇总捕获率; `--json` 可选输出机器可读文件(人工 diff 用, 无门禁)。退出码语义: 脚本自身失败非零; MISS 不红(用户裁定) | scripts/ | ☑ 2026-09-08: 匹配器三通道(id 前缀/warning code/issue code)+NO-MATCHER 诚实非零+gap 提前 PASS/covered MISS 双漂移警示; 实测 covered 18/18 PASS、gap 8/8 MISS、退出码 0。偏差注记(评审 P2): 种子收敛为 3 章+1 清洁 Scene(结构资产改逐条目注入, 见 catalog.base_seed); 表列省略 detector 列(detector/face 留在 catalog) |
| A2-1.3 | 文档收口: N54 追记实施落点; 交接 §7; README 一行(bench:continuity 跑法) | docs | ☑ 2026-09-08 |

验收: bench:continuity 跑通; covered 全 PASS、gap 全 MISS(诚实呈现)、llm-only 带标注;
npm test + typecheck + check:* 全绿(本批不动 src, 应零影响)。

### 批 2: 缺口补确定性检测器(core 加法)

| # | 任务 | 层/落点 | 状态 |
|---|---|---|---|
| A2-2.1 | 章序一致性 3 项(reveal 章<plant 章; thread start>end 或越界实际章数; arc range 不含其 thread range)→ risk 面 | assistant 新文件 `radar-continuity.ts`(输入 storyMap snapshot, 纯函数) | ☑ 2026-09-08: 落地偏差注记——reveal_stages/planned_seed_chapter 不在 StoryMapAsset, 收集器直读结构资产 frontmatter(同 ingest 雷达读 import-log 先例), storyMap 仅供边/场景/章聚合 |
| A2-2.2 | 悬空/孤儿 4 项(thread 无 scene/章挂靠; reveal.target_id 悬空; planned_payoff_scene 悬空; N17 legacy 投影边悬空——扫 storyMap edges 全集而非仅 index.relations)→ risk 面 | 同上新文件, 不改 radar-risk.ts 既有行为 | ☑ 2026-09-08: legacy_edge_dangling 实现 = storyMap edges 全集减显式 relations 子集(显式边悬空归 radar-risk 既有 risk-dangling-, 同问题不双报, 判别测试锁定); thread_orphan=无 serves_thread 边(note), 与 structure_unassigned 判别(批 1 评审注记落实); pays_off_in_scene 边归 payoff_scene_dangling 归属不重复报 |
| A2-2.3 | scene_index 冲突(同章 index 重复/乱序)→ health 面 | outline 新导出 + assistant/health 加法接线(实施时按依赖拓扑定具体文件) | ☑ 2026-09-08: outline `sceneIndexConflicts` 纯函数 + health 章级信号(id `health-scene_index_conflict-chapter-<n>`, severity risk); 语义裁定=scene_index 是排序键, 任意排列合法、唯一性破坏才是冲突——「乱序」不报(记 N54 追记) |
| A2-2.4 | 行为测试: 每检测器正例+负例(正常书零信号、不误报), 断言注释引 N54; 对账测试(logical_key 稳定/重开不复活)走既有 reconcile 契约 | 各包 test/ | ☑ 2026-09-08: assistant `test/radar-continuity.test.ts` 10 用例(七检测器正负例+幂等/双向对账含 reopen+双路 sweep 接线+legacy 与 risk-dangling 判别+scene_index_conflict 正负例; 评审轮补多边 reveal/arc 双认/未来章不报用例断言)+ outline sceneIndexConflicts 单测; 基准基础种子零信号自检兜底「正常书零信号」 |
| A2-2.5 | dsh 核对: EVENT_RADAR_MAP 既有 adopt/generate→risk/health 映射是否覆盖新检测重扫; 缺则补一处映射(预期零改动) | dsh/src/radar-hooks.ts | ☑ 2026-09-08: 核实 radar-hooks 经 runRadarSweepAtomic→collectGroup 消费 risk/health 面, 连续性命中已并入两路(collectGroup + scanRiskContinuityRadar), dsh 零改动 |
| A2-2.6 | catalog 更新: 8 条 gap 改期望为捕获, 重跑 bench:continuity 展示捕获率上升(报告 diff, 无门禁) | evals/ | ☑ 2026-09-08: 8 条改类 covered, 实测 covered 26/26 PASS(批 1 时 18/18, 捕获率 18→26), gap 0/0, 退出码 0 |

验收: 新检测器全带正负例测试; npm test + typecheck + check:git-writers/
check:distribution/check:audit-gate 全绿; bench:continuity 矩阵 gap 类目全 PASS;
信号经既有收件箱/reconcile 路径落盘(无新 git writer)。

### 批次与提交协议

批 0(计划入册, 本节+N54+交接 §7 一提交)→ 批 1 → 批 2, 各自: 独立评审代理 review →
修复 → 单独提交(带 Agent Note)→ push origin main。会话预算不足时诚实切小子批次
(gap 8 条可按 2.1/2.2/2.3 三切)。全程只在本仓 main, 不触碰父仓。

### 明确不做(防回流)

- 不建日期/时间戳级 timeline 模型(§6.18.6「统一 timeline service 不做」, 本批只做
  章序一致性)。
- 不做 LLM 语义捕获断言/付费评测(N52 待裁决; llm-only 类只标注)。
- 不设 CI 硬门禁(用户裁定 2026-09-08; 未来收紧为可选后续)。
- 不建第二事件流/新信号 schema/新雷达面/新工具。
- 不做性能基准(§4b 另一候选, 不混批)。


## 4e. M13-C: deep_import 后台 job 托管 + 混合完成通知(N55, 2026-09-08) — 计划已入册待实施

> 依据: 2026-09-08 wiki 调研头脑风暴 A3 方向(优秀范式 9「task-owned signal/非唤醒通知」/
> 46「job 完成通告最后发」)+ rc.1 宿主 seam 逐字核实(`ctx.jobs`
> start/list/get/read/kill/wait/onJobDone; `Agent.followup` 唤醒/`Agent.inject` 非唤醒;
> radar.ts job 先例)+ 用户混合通知裁定。**ADR-0023 §3 本就把 deep_import 定为「显式长 job
> 由 Node 托管, 不因浏览器断线/watcher 取消」——本批是把实现补齐到 ADR 既定形态, 非新架构**;
> ADR-0022 durable driver 语义(checkpoint/四窗口/reauthorize/apply 状态机/allowed-once)
> 全部不变, 仅执行宿主从工具同步栈换 job。裁定全文见 `specs/adjudications.md` N55。

### 0. 插件边界纪律(M13-C 特化, 统领本批)

1. **core/imports 零改动**: `deepImport()` 入口已带 (service, agent, root, opts, signal,
   profile) 签名, job 只换执行宿主; durable driver/审批/事务路径不动。
2. **dsh 新文件加法**: `jobs/deep-import-job.ts` 新 job kind(JobKindMap 声明合并, 照
   radar.ts); 工具 execute 仍只经 novelcraftToolFactory + capabilities.*, 不绕 factory。
3. **审批语义不进 job**: adopt/reauthorize 仍走 ApprovalGate allowed-once(ADR-0022 §7);
   job 只是执行载体, kill/abandon 不产生任何审批副作用。
4. **无新工具/无新 git writer**: 39 工具与 MATRIX/preset 清单不动; README seam 矩阵与交接
   §4.2 口径更新(jobs 从 radar 单 kind 变两 kind)。
5. **模型可见⟺可回放**: 三入口工具回执从阻塞结果改为句柄属模型可见变更——工具描述与
   output schema 同步, 行为测试锁定新回执; trace 词表加法(job_started/job_finished/
   job_notify)进 contract 断言。

### 任务表

| # | 任务 | 层/落点 | 状态 |
|---|---|---|---|
| C1 | job 骨架: `dsh/src/jobs/deep-import-job.ts`——JobKindMap 合并 `'novelcraft-deep-import'`; JobStart{kind, label, owner: 发起 agent, outputLimitBytes 有界}; run(): new AbortController + work=`deepImport(service, agent, root, opts, signal, profile)`; done settle `{status, detail, output≤limit}` | dsh 新文件(照 radar.ts) | ☐ |
| C2 | 三入口切换: `novelcraft_deep_import`(tools.ts)与 `workflow_resume/start_new`(workflow-face.ts) execute 改为启动 job 后**立即返回句柄** `{jobId, workflowId, label, mode}` + workflow_inspect 查进度指引; 撤销 3_600_000 超时覆盖; `workflow_abandon` 先 jobs.kill → 有界 wait 至终态再走既有 R17 洁净清理, 未到终态如实返回「kill 已请求、清理待终态」(不冒充完成) | dsh tools/workflow | ☐ |
| C3 | 混合通知(用户裁定): `ctx.effect(()=>jobs.onJobDone(...))` 按 kind 过滤——completed/failed→`owner.followup`(唤醒, 确定性摘要: workflowId/status/批进度/待审批项数, 宿主 bash job 同款模式); killed→`owner.inject`(非唤醒记录性通知); 无 owner 静默 | dsh job 模块 | ☐ |
| C4 | 防重入: 模块内 Map(root+workflowId→jobId), 已 running 拒新 start(fail-closed 报既有 jobId); 重启后 Map 空, 恢复走 ADR-0022 既有四窗口 | dsh job 模块 | ☐ |
| C5 | trace 加法: 词表新增 job_started/job_finished/job_notify(followup\|inject), contract 断言 | trace + dsh | ☐ |
| C6 | 行为测试(照 radar job 测试 harness): 句柄即返/完成 followup/killed inject/无 owner 静默/防重入拒/abandon→kill 排队清理/owner=agent 不随浏览器断开取消(ADR-0023 §3)/FakeApproval 三态语义不变 | dsh test | ☐ |
| C7 | 文档: N55 追记实施落点; dsh README(jobs 两 kind/通知形态/seam 矩阵); 交接 §4.2/§7; 台账 §6.24 P0 长任务行注记 | docs | ☐ |

验收: deep_import 不再阻塞会话(句柄即返, 1h 超时撤销); 通知四路径行为测试; 39 工具面不变;
npm test + typecheck + check:* 全绿; trace contract 断言新事件。

### 明确不做(防回流)

- 不做通用 operation center/跨 job 进度页(台账 §6.24.1 既有边界)。
- 不做 job 级持久注册表/重启自动续跑(ADR-0022 恢复语义已覆盖, 重启后由 inspect/resume 驱动)。
- 不把 job 状态写入 session 事件(rc.1 session 词表无 job 事件; 自定义 SessionEvent 有迁移
  边缘陷阱, 见 N56)。
- 不动 resume/start_new 的审批与对账语义(N40 三重前置/执行后对账保持)。

## 4f. M13-D: 作者状态面投影——探测裁定不做(N56, 2026-09-08)

> 依据: 2026-09-08 wiki 调研头脑风暴 A4 方向(优秀范式 53「投影声明五要素+三铁律」/19
> 「投影读状态正规姿势」)。宿主 seam 逐字探测结论: rc.1 `dsh-session-projection` 完整存在
> (ProjectionDefinition key/stateSchema/init/apply/wire/stateVersion + ctx.sessionProjections
> register/onChanged/snapshot), **但投影的事件源严格限定为 session 日志事件**
> (`KNOWN_SESSION_EVENT_TYPES` 27 大类中无 job 事件, dsh-jobs 对 SessionEventMap 零声明合并);
> novelcraft 领域状态(工作流/收件箱/雷达)全部在 vault 文件与进程内 job, 不进 session 日志——
> 投影 fold 不到任何 novelcraft 领域状态。用户裁定(2026-09-08): **暂缓, 只记否定性探测裁定**
> (照 N52 主题探测「无 seam → truthful 不做」先例); client 保留现有 3s 轮询 + focus/visibility
> 刷新。重开条件与 Alternatives considered 见 `specs/adjudications.md` N56。本节无实施批次。

## 4g. M13-E: 工具面 token 经济学与文档(N57, 2026-09-08) — 计划已入册待实施

> 依据: 2026-09-08 wiki 调研头脑风暴 A5 方向(优秀范式 64「Model Experience 三连: What the
> model sees/Token effect/KV cache effect」/52「tools() 暴露面」)+ rc.1 逐字核实(dsh-tools
> 无 per-tool visible 字段, 但有 `restrict(allow/deny)` 与 `SystemPrompt.tools()` 两个可见性
> 通道; 组开关=注册期过滤, 默认全开——「profile 即产品」)+ 用户裁定 v1 保持全组默认开。
> 裁定全文见 `specs/adjudications.md` N57。

### 0. 插件边界纪律(M13-E 特化, 统领本批)

1. **零运行时行为变更**: 本批不动工具注册/暴露/组开关的任何运行时路径; E1 脚本仅报告不进门禁。
2. **E1 有意识偏离「脚本零 @deepseek-ai import」先例**: 计量对象本就是 dsh 工具面, 脚本
   import workspace `@novelcraft/dsh` 构建产物读 buildToolSegments(仅 devDep 环境, 不进
   CI); 与 benchmark-read-paths/continuity-benchmark 的 core-only 纪律(N54 ③)不冲突——
   后者计量 core。
3. **文档随代码走**: README/skill 修正与 E1 数字同批, 不留「宣称与计量不一致」窗口。

### 任务表

| # | 任务 | 层/落点 | 状态 |
|---|---|---|---|
| E1 | 计量脚本 `scripts/tool-token-report.mjs` + `npm run tools:report`: 读 buildToolSegments, 逐工具/逐组对 schema+description 序列化后经 `estimateContextTokens`(@novelcraft/context)计量, console.table 输出工具×schema tokens×组小计, `--json` 可选 | scripts/ | ☐ |
| E2 | 文档: dsh README 六组工具补 Model Experience(每组 What the model sees/Token effect/KV cache effect, 数字来自 E1); **修复漂移: preset skills「19 个领域工具」→39(novelcraft-core 与 map skill 两处)** | README + preset skills | ☐ |
| E3 | N57 裁定固化: v1 全组默认开; 不引入 restrict()/SystemPrompt.tools() 动态分域(工具 schema 是 prompt 前缀一部分, 按回合变动破坏 KV-cache 前缀稳定+可回放简单性); 重开条件=E1 数字显示显著收益且用户再裁定 | specs/adjudications.md | ☑ 入册即固化 |

验收: tools:report 跑通且覆盖 39 工具与六组一致; README/skill 数字一致; npm test + typecheck
全绿(本批不动 src 运行时; preset 测试若锁工具数口径须同步)。

### 明确不做(防回流)

- 不做按创作阶段/会话状态的动态工具暴露(N57 重开条件未满足前不评估)。
- 不建工具选择策略引擎/自动组装配。


## 5. 明确不做(已裁定防回流)

RP 私域(D23, R6 后)、对象图片(D19)、带批注 .docx(D20)、站内生图(ADR-0020)、
多用户/云同步、统一 timeline service(§6.18.6)、回收站(§6.13.6)。

## 6. 风险与闸门

- A1 是闸门: schema 注入模式取决于宿主 seam 能力, 先探测后裁定(记 N 裁定)。
- B3 job 托管须对读 ADR-0023 执行节; 新增 job kind 遵守单次补跑。
- M12 各项依赖 Track A 收口后的 llm_step 信任面。
- 任何共享层触碰只走 ADR-0018 四条纪律(登记/上游回馈/去 fork 化/测试);
  能不触碰就不触碰。

## 7. 计划维护协议

- 每完成一个任务: 勾选状态 + 记实际落点 commit; 批次完成时更新 STATUS-M4 与台账切片。
- 方向性变更(增删批次、改变能力归位)须在 specs/adjudications.md 记 N 裁定并同步本计划。
- M11/M12 开工前把该批次细化到任务级(照 M10 格式)。

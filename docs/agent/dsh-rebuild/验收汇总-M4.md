# M4 重构验收汇总(2026-08-14, Round 18 快照)

> **历史快照**: 本文件是 Round 18 时点的事实快照, 其中「旧引擎未退役 / 旧 FastAPI/PG 仍在
> origin/main / 192 测试 / client UI 未实现」等是当时状态, **不是当前事实**。当前唯一真相
> 口径以 `STATUS-M4.md` 与 `跨会话交接.md` 为准(旧引擎已退役归档 tag `old-engine`,
> 266 测试全绿, client UI 已实现)。
>
> 性质: 当前状态的事实快照(防幻觉)。完成与未完成都明确标注。
> 依据: ADR-0016、设计文档 D1–D27、specs/adjudications.md(26 + 13 条裁定)。

## 一、阶段完成矩阵

| 阶段 | 交付物 | 状态 |
|---|---|---|
| R0 Spec 提取 | specs/assets(6 份)+ prompts/catalog(32 spec)+ rules(store-rules R1–R64 + policy-defaults)+ 两批裁定 | ✅ |
| R1 内核 | @novelcraft/vault(29 测试, dist 发布)+ @novelcraft/store(68 测试, R1–R64 可测全覆盖) | ✅ |
| R2 内容手 | @novelcraft/llm-step(19 测试: 自实现 schema 校验/4 spec/policy 覆盖链/预算/超时重试/journal/text 输出) | ✅ |
| R3 写作切片 | @novelcraft/writing(15 测试 + r3-demo 闭环: 停靠→审查→返修→采用) | ✅ |
| R4 深度导入 | @novelcraft/imports(19 测试 + r4a/r4b demo: 六阶段 + L0–L3 去重 + 恢复) | ✅ |
| R5 领域包 | world(6)/outline(7)/memory(5)/context(5)/rag(4) | ✅ |
| R6 assistant | 信号/收件箱/校准/微工作流目录(15 测试) | ✅ 核心; radar 调度 = 挂载阶段 |
| R7 client + 发布 | preset(skills 9 册 M4 校对 + presets 4 套)+ starter(安装文档 + 兼容矩阵); **client UI 未实现**(阻塞: DSH 源码 checkout, client/README.md 已记录) | 部分 |

## 二、测试矩阵(当前全绿)

| 包 | 测试 | 包 | 测试 |
|---|---|---|---|
| vault | 29 | world | 6 |
| store | 68 | outline | 7 |
| llm-step | 19 | memory | 5 |
| writing | 15 | context | 5 |
| imports | 19 | rag | 4 |
| assistant | 15 | **合计** | **192** |

根 `npm test --workspaces` 全绿; `npm run typecheck --workspaces` 零错误;
vault/store 有 dist(Node 可加载, 相对导入 .js 化)。

## 三、决策与裁定记录(可追溯链)

- 设计文档决策表: **D1–D27**(模型分工/守望/去重/写作台/开源/单用户/M4 终态/18+8 条规格裁定)
- specs/adjudications.md: 第一批 18 条 + 第二批 N1–N13(落点/命名/默认值/字段名)
- ADR: 0016(Accepted; dsh-rebuild 侧为 0015, 侧车 0014 已标 Superseded)

## 四、对全局 DoD(设计文档 §25.1)的对照(诚实)

| DoD 条目 | 状态 |
|---|---|
| ① 一键装完「新建书→拖文件→深度导入→复核→参照→修订→守望」全链跑通 | ❌ 未达成——需挂载阶段(13 包 Cordis 适配器)+ client UI |
| ② 数据语义等价: R0 规则有行为契约测试 | ✅ 192 测试覆盖 R1–R64 可测规则 |
| ③ 编排纪律: trace contract 全绿 | ✅ `@novelcraft/trace` + `imports` 的 `runDeepImport` + trace-contract 测试(见 docs/agent/dsh-rebuild/trace-contract-验收.md) |
| ④ 开源面: starter 一键安装 + mock 面随包发布 | ⚠️ 文档已备(starter/README + seam 契约); 未发布 npm |
| ⑤ 旧引擎退役(R4 后) | ❌ 未发生——旧 FastAPI/PG 仍在 origin/main; 退役是合并后的仓库动作 |

## 五、明确剩余项(全部有记录, 无隐性缺口)

1. **Cordis 挂载阶段**: 13 包 → DSH 服务插件的 seam 适配器(契约已定稿于
   packages/novelcraft/README.md); 依赖 DSH rc.6 插件开发环境。
2. **client UI**: 宠物/收件箱/写作台 client-module(阻塞: DSH 源码 checkout 构建链,
   client/README.md)。
3. 实现期备忘 4 条(设计文档 §18): 文本附件通道实测 / rag 嵌入后端选型 /
   多模型预设保存落点 / DSH 升级窗口流程。
4. ~~trace contract 测试框架(§15 已设计, 未实现)~~ ✅ 已实现(见 trace-contract-验收.md)。
5. 合并策略: codex/m4-dsh-plugin-rewrite 分支(27+ commits)最终如何入 main
   (旧引擎退役/文档联动), 待用户裁决。

## 六、下一步候选(排序)

A. 用户评审本汇总 → 裁决剩余项节奏(挂载阶段 vs client vs 合并策略);
B. 启动挂载阶段(需 DSH rc.6 插件开发环境, 可复用旧 dsh-plugin 的构建/冒烟骨架);
C. ~~实现 trace contract 测试框架~~ ✅ 已完成(trace-contract-验收.md)。

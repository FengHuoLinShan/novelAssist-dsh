# 旧引擎退役 验收快照

- 日期: 2026-08-14(novelAssist-dsh / main)
- 范围: 从 novelAssist-dsh 仓库移除随迁出携带的旧 FastAPI/PG/Vue 引擎, 使仓库成为纯
  M4 DSH 插件 monorepo。
- 归档: annotated tag `old-engine`(指向退役前 HEAD `08e38e63`), 任何时刻
  `git checkout old-engine -- <path>` 可只读取回旧实现。

## 删除清单

| 类别 | 内容 |
|---|---|
| 后端 | `backend/`(FastAPI/Alembic/uv, 1116 文件) |
| 前端 | `frontend-console/`(Vue 3 SFC, 468 文件) |
| 部署/运维 | `deploy/`、`docker/`、`docker-compose.yml`、`Makefile`、`start.sh`、`.dockerignore` |
| 工具/工作流 | `tools/`(deepseek_scene_probe)、`workflows/`(context-snapshot)、旧 `scripts/*.py` |
| 旧文档 | `docs/` 下 architecture/modules/frontend/product/prompts/acceptance/… 与旧 ADR(0001–0014 及主题 ADR); 顶层 CONTEXT/DECISIONS/development-guide/testing-guide/NOTES/SECURITY/THIRD_PARTY_LICENSES |
| 旧工具链 | `.github`(旧 CI: backend/frontend/production-image/codeql/architecture-docs + dependabot 旧配置)、`.claude`、`.opencode`、`.superpowers`、`.playwright-mcp` |

## 保留清单(M4)

- `packages/novelcraft/`(15 包)、`specs/`、`starter/`、`scripts/m5-*.mjs` + `r3/r4-demo.mjs`
- `docs/adr/0016|0017`、`docs/agent/dsh-rebuild/`、`docs/agent/dsh-upstream/`(DSH 参考)
- 根 `package.json`/`package-lock.json`、`.gitignore`、`.gitattributes`、`LICENSE`

## 重写

- `README.md` → novelAssist-dsh M4 简介 + 结构/核心原则/快速开始
- `AGENTS.md` → M4 硬约束(核心包零 DSH 依赖 / 文件唯一真相 / adopt 必过 approval / trace contract 等)
- `CLAUDE.md` → M4 包结构 + 常用命令 + seam 契约导航
- `docs/adr/README.md` → 最小 M4 ADR 索引(0016/0017/0018)
- `.github/workflows/ci.yml` → 最小 CI(npm ci + 拓扑序构建 + test + typecheck)

## 验证

- `npm test` 266 全绿 / `npm run typecheck` 零错误(删除旧引擎后 M4 包无回归)
- 交叉引用已核查: M4 代码不依赖旧引擎, 旧引擎不依赖 M4。

## 剩余

- 旧引擎退役(ai-writing-assist main): 父仓库 main 的旧引擎保留, 退役时机另行裁决(ADR-0017 §1)。
  **【用户明确指示】不得反向改动父仓库 ai-writing-assist(含其 main)** —— 只读, 不回写/不退役/不同步。

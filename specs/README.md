# R0 · Spec 提取产物(specs/)

M4 重写(ADR-0016)的 R0 阶段产物: 从旧 Python 代码(origin/main, a257df23e)+ docs/prompts
提取三份规格, 作为 TS 插件重写的唯一需求来源。旧代码此后只作 Spec, 不作运行时代码。

## 裁定

`adjudications.md` = 全部【待定】标注的最终裁定(18 条, 用户已确认, D26)。
spec 文件内的【待定】以裁定记录为准。

## 目录

- `assets/<module>.md` — 资产 schema/frontmatter 定义(字段/类型/必填/状态机/旧表映射/完整性规则)
- `prompts/<spec>.md` — llm_step 的 prompt/spec 资源(输出 schema/预算/温度/超时/降级条款)
- `rules/<module>.md` — 完整性规则目录(merge/去重/CAS/世界书 lifecycle/降级条款)

## 提取约定(所有子代理必须遵守)

1. **只写规格, 不写代码**; 每条结论标注来源文件路径(可加行号区间)。
2. **不确定处标【待定】**, 不得臆造。
3. 覆盖范围 = **创作资产与状态机**, 不覆盖 API 请求/响应信封(那是 M1/M2 过程形态的事)。
4. 字段表统一四列: 字段 | 类型 | 必填 | 语义。
5. 旧表映射列统一格式: `表名.列名`。
6. 中文书写; 用户语言优先, raw ID/JSON 只出现在字段表的技术列。
7. 每个文件头部标注: 来源 commit `a257df23e`、提取日期、提取范围(文件列表)。
